# Server Configuration
PORT=3000
SMTP_PORT=25

# Domain Configuration
# Domains that can receive emails (comma-separated)
ALLOWED_DOMAINS=mailhook.local,callback.local

# Database
DB_PATH=./data/mailhook.db
